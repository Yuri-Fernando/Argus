"""api/ — FastAPI service exposing the customer-intelligence data surface as REST/JSON.

See `api/README.md` for the route list and why this intentionally duplicates `mcp/`'s data
surface at the protocol boundary (ARCHITECTURE.md §20).
"""
