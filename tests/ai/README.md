# tests/ai/

Agent evaluation harnesses — placeholder until **Sprint 14** (`agents/`: Customer Intelligence, Data Quality, Recommendation, Monitoring agents; LangGraph + human-in-the-loop, ROADMAP.md) lands. Once non-trivial, this directory exercises the golden question sets and LLM-as-judge scoring defined in `agents/*/evaluation/` (owned by the agentic-AI workstream, ARCHITECTURE.md §15), plus the prompt-injection test cases called out in that same section's "AI security" note. The single acceptance criterion this category exists to prove: *"the Recommendation Agent's suggestion queue requires explicit approval before any downstream action is logged as `executed`"* — i.e. that the human-in-the-loop gate (ADR-006) cannot be bypassed by any agent path, adversarial prompt included.
