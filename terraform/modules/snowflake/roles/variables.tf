# No inputs today — role names are fixed conventions (ECI_*) shared by
# every environment. Kept as an empty file rather than omitted so the
# main.tf/variables.tf/outputs.tf/README.md pattern stays consistent
# across every module in this repository.
