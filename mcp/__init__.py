"""Custom MCP server package — see mcp/README.md and ARCHITECTURE.md §15.

Note: this package's own name (`mcp`) collides with the third-party `mcp` SDK package. See the
"Known constraint" section in mcp/README.md before changing how modules in this package are
invoked or imported.
"""
