"""agents/a2a/ — Agent2Agent (A2A) protocol layer wrapping the four agents in `agents/`.

See agent_card.py's module docstring for the design rationale, server.py for the FastAPI app,
and client.py for a minimal discover-then-send-task client. Built in Sprint 17 (ROADMAP.md).
"""
