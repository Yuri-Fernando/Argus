# Secrets
.env
*.pem
*.key
.kaggle/kaggle.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Data (generated/downloaded — never committed, see DATA_MODEL.md for how to regenerate)
data/raw/**
data/synthetic/**/*.csv
data/documents/*.md
data/documents/*.pdf
data/documents/fiscal/*.png
!data/synthetic/generators/
!data/synthetic/config.yaml
!data/synthetic/generate_all.py
!data/synthetic/__init__.py

# Data — pipeline-derived (generated by lakehouse/mdm/ml/snowflake pipelines, never committed;
# regenerate with `python lakehouse/run_pipeline.py`, `make mdm`, `python -m ml.features.build_features`
# && `python -m ml.churn.train` && ..., `python snowflake/local_runner.py` — see BUILD_LOG.md)
data/lakehouse/
data/mdm/
data/ml/
data/warehouse/
data/rag/chroma/
data_quality/reports/*.json
data_quality/reports/*.md

# MLflow
mlflow/mlruns/
mlflow/mlartifacts/

# Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
!*.tfvars.example
.terraform.lock.hcl

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# GX
data_quality/reports/*.html
great_expectations/uncommitted/

# Docker
docker-compose.override.yml

# camada enterprise v2 — bancos SQLite de runtime
**/outbox.db
**/cqrs*.db
