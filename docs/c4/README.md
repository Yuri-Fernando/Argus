# C4 Model — Argus

Diagramas em [Mermaid](https://mermaid.js.org/) (renderizam direto no
GitHub). Níveis: **Context** → **Container** → **Component**. Código
(nível 4) fica no próprio código-fonte + `services/*/README.md`.
